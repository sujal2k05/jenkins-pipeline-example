pipeline {
    agent any

    stages {

        stage('Initialize') {
            steps {
                echo 'Pipeline started'
            }
        }

        stage('Build') {
            steps {
                echo 'Building project'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests'
            }
        }

    }
}
